// STUB — будет реализовано в GREEN-коммите Task 1.
export interface RuForms {
  one: string;
  few: string;
  many: string;
}

export function pluralizeRu(_n: number, _forms: RuForms): string {
  return '';
}

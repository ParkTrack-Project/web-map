// Vitest global setup file (forward-declared in vite.config.ts).
// Plan 02 will populate this with @testing-library/jest-dom matchers and MSW server bootstrap.
import '@testing-library/jest-dom/vitest';
